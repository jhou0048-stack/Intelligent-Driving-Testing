# Scripts

This directory is reserved for small developer and automation entry points, such as environment checks or test-suite launchers.

Reusable logic belongs in the appropriate package under `src/`; scripts should only coordinate that logic.

